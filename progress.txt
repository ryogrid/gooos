## Codebase Patterns
- Use `//go:linkname` with bodyless Go function declarations to call assembly stubs from Go code
- outb/inb stubs in stubs.S follow SysV AMD64 ABI (rdi=first arg, rsi=second arg, rax=return)
- QEMU direct `-kernel` boot fails for ELF64; use ISO boot via `make iso` + `make run` instead
- Serial output uses COM1 (0x3F8) at 115200 baud 8N1, verified with `-serial stdio`
- TinyGo builds the entire `./src` package, so new `.go` files are automatically included
- IDT descriptor for lidt must be packed (2-byte limit + 8-byte base = 10 bytes); use [10]byte array in Go since structs get alignment padding
- To get an assembly function's address in Go, create a helper that returns it via `leaq symbol(%rip), %rax`
- GDT64_CODE selector = 0x08 (second GDT entry at offset 8)
- ISR stubs use ISR_NOERR/ISR_ERR macros; vectors with CPU error code: 8, 10-14, 17, 21
- .altmacro + .rept + %COUNTER pattern generates bulk assembly stubs (used for vectors 32-255 and the ISR address table)
- ISR address table in .rodata: array of 256 .quad pointers, accessed from Go via isrTableAddr()
- go_interrupt_handler is exported with //export for C-ABI calling from assembly
- Stack alignment before Go call: save RSP in callee-saved register, andq $-16 to align, restore after call
- PIC remap: master PIC 0x20/0x21, slave PIC 0xA0/0xA1; ICW1-ICW4 sequence; master offset 32, slave offset 40
- Always register default IRQ handlers (32-47) that send EOI to prevent PIC hanging on spurious interrupts
- After sti, add hlt loop at end of main() to keep kernel alive while handling interrupts
- PIT channel 0: command 0x34 (channel 0, lobyte/hibyte, rate generator mode 2, binary); divisor = 1193182/100 = 11932
- Register PIT handler AFTER default IRQ handlers, so it overrides the default for vector 32
- PS/2 keyboard: read scancode from port 0x60 in IRQ1 handler; bit 7 set = key release (ignore); scancode set 1 for make codes
- VGA echo buffer pattern: maintain a byte array + length, redraw entire line on change, clear trailing chars with vgaClearLine
- go_interrupt_handler accepts (vector, errorCode); error code stored in lastErrorCode global for handlers to read
- CR2/CR3/invlpg stubs in stubs.S; readCR3() &^ 0xFFF gets PML4 base; identity mapping means phys=virt for page tables
- Page frame allocator: bump allocator starting at _heap_end aligned to 4 KiB; heapEndAddr() stub returns linker symbol value
- mapPage walks 4-level page table (PML4→PDP→PD→PT); does NOT split existing 2 MiB huge pages
- For VM demo, use virtual addresses outside the boot-time 1 GiB identity map (e.g., 0x40000000) to avoid huge page conflicts
- Context switch: save/restore callee-saved regs (rbx, rbp, r12-r15) + RSP; new task stacks need zeroed regs + entry address + halt safety net
- New tasks start from inside a timer ISR (IF=0); must call sti() first to re-enable interrupts
- Assembly address helpers (leaq symbol(%rip), %rax; ret) get //export Go function addresses for task entry points
- Schedule from timer handler: EOI first, then switchContext; interrupt gates keep IF=0 during the switch
- isr_common's %rbx (pre-alignment RSP) is preserved across context switches because switchContext saves/restores callee-saved regs
- GDT can be rebuilt in Go at runtime: define [N]uint64 array, pack TSS descriptor as two entries, lgdt + far return to reload CS, ltr loads TSS
- TSS RSP0 must point to kernel stack for Ring 3→Ring 0 transitions; CPU loads RSP from TSS.RSP0 on privilege change
- User-accessible pages need pageUser (bit 2) set at ALL page table levels (PML4E, PDPE, PDE, PTE); walkOrCreate propagates User flag
- IDT gate DPL=3 (TypeAttr=0xEE) required for int 0x80 from Ring 3; DPL=0 gates cause #GP from user mode
- iretq to Ring 3: push SS(0x23), RSP, RFLAGS, CS(0x1B), RIP; user CS = 0x18|RPL=3, user SS = 0x20|RPL=3
- User code pages mapped at vaddr > 1 GiB (outside boot identity map) with pagePresent|pageWrite|pageUser flags
- After jumpToRing3, task 0 runs in Ring 3; timer preempts via TSS RSP0 kernel stack; scheduler context-switches normally
- In-memory FS: global `fs` FileSystem with flat [16]FileEntry array; each entry has fixed [4096]byte data, name string, size int, used bool
- VGA line assignments after US-011: serial 0, IDT 1, ISR 2, PIT 3, kbd 4, ints 5, GC 6-8, VM 9, FS 10, timer 11, GDT 12, kbd-echo 12, sched 13-14, tasks 15-17, user 18, SMP 19
- AP trampoline lives in .rodata, copied to 0x8000 at runtime; uses hardcoded absolute addresses assuming TRAMP_BASE=0x8000
- LAPIC at 0xFEE00000 must be identity-mapped via mapPage (outside boot 1 GiB range); use pagePCD|pagePWT for MMIO
- ACPI RSDP found by scanning BIOS ROM area 0xE0000-0xFFFFF (16-byte aligned); RSDT→MADT gives AP count
- INIT-SIPI-SIPI: INIT IPI (ICR=0xC4500), wait 10ms, SIPI×2 (ICR=0xC4608 for vector 0x08=0x8000/0x1000)
- APs share BSP's PML4 (same CR3); trampoline GDT has null+code32+data+code64 entries for mode transitions
- AP entry function must avoid heap allocation (GC not thread-safe); use serialPutChar for output
- lock xaddl atomically claims AP index from shared counter; each AP loads its stack from a pre-indexed array
- After LAPIC enable (SVR bit 8), configure LVT LINT0=ExtINT to preserve PIC interrupt delivery to BSP

- freePage/allocPage free-list: first 8 bytes of a free page store the next-pointer; freeListHead is the list head
- WaitQueue: fixed-size [16]uint32 FIFO of task IDs; waitQueueSleep blocks caller, wakeOne/wakeAll are interrupt-safe
- schedule() only sets old task to taskReady if it was taskRunning — preserves taskBlocked state for waitQueueSleep callers
- yield() sets current task to taskReady and calls schedule() — same pattern as waitQueueSleep but without blocking
- taskSleep(ticks) uses a sorted sleep queue (ascending by WakeupTick); timer handler wakes expired entries before schedule()
- Sleep queue is a fixed-size [16]uint32 array of task IDs; sorted insert + shift on wake keeps it ordered
- Channel: static pool of [32]Channel; chanCreate returns *Channel from pool; ring buffer max 256 uintptr slots
- chanSend/chanRecv block via waitQueueSleep when buffer full/empty; chanTrySend is non-blocking (interrupt-safe)
- Unbuffered channels (capacity=0) use buf[0] + count as a single-slot handoff; sender/receiver coordinate via WaitQueues
- Spawn receiver task before sender for unbuffered channels so receiver blocks first, ensuring clean rendezvous
- selectWait: register on all WQs before blocking, remove from all WQs after wakeup, loop back to re-check readiness (handles spurious wakeups)
- waitQueueAppend/waitQueueRemove are non-blocking WQ helpers for select; append adds without changing task state, remove deregisters by task ID
- chanRecvReady/chanSendReady check readiness without side effects; chanRecvDirect/chanSendDirect execute without blocking (assume ready)
- Microkernel service pattern: IRQ handler uses chanTrySend (non-blocking) to publish events to a channel; dedicated consumer task calls chanRecv in a loop to process them
- For request-response over channels: use static pools for Request/Response structs, pass unsafe.Pointer as uintptr, reply on per-request channel (capacity 1)
- fsSend* helpers each create a reply channel, send request pointer, and block on reply — clean RPC-over-channel pattern
- Pack small structs into uintptr for channel transport: e.g., KeyEvent = scancode | (ascii << 8)
- keyboardChannel capacity=16; IRQ1 handler drops events if full (chanTrySend returns false)
- serialSend uses a static [16]string pool + uintptr(unsafe.Pointer(&slot)) to pass string pointers through channels safely
- Direct serialPrint/serialPrintln remain for interrupt context and early boot; serialSend is for task context only
- isr_common passes register frame pointer as 3rd arg (%rdx) to go_interrupt_handler; pre-alignment RSP = pointer to saved GPR frame
- SyscallFrame struct maps the isr_common stack layout: R15(+0)..RAX(+112), Vector(+120), ErrorCode(+128), RIP(+136)..SS(+168)
- Vector 0x80 is special-cased in go_interrupt_handler for syscall dispatch (not the handler table)
- Syscall ABI: RAX=number, RDI/RSI/RDX/R10/R8/R9=args, return value written to frame.RAX; frame writes propagate back through iretq
- taskExited (state 3): task is never rescheduled; schedule() skips it because it only matches taskReady
- sys_print writes user buffer byte-by-byte via serialPutChar — safe from interrupt context (int 0x80 handler runs with IF=0)
- ELF64 header: 64 bytes; program header: 56 bytes; fields at fixed offsets read with readU16LE/readU32LE/readU64LE helpers
- Avoid append() for large struct slices — TinyGo's sliceAppend needs memmove (not provided in stubs.S); use static fixed-size arrays instead
- elfParse returns a slice backed by a static [16]Elf64Phdr buffer — caller must consume before next call
- elfLoad uses fsSendRead (channel-based) not direct fsRead — requires schedReady=true and FS task spawned; ELF binary must be pre-stored via direct fsCreate/fsWrite
- Writing to newly mapped virtual addresses works without invlpg — new mappings cause TLB miss, CPU walks page table; only need invlpg for CHANGED mappings
- ELF user stack at 0x7FFF0000 (8 KiB, 2 pages); code vaddr from ELF headers (e.g., 0x40010000); both outside 1 GiB identity map
---

## 2026-04-14 - US-001
- Implemented page-frame free-list allocator: freeListHead global, freePage() push, allocPage() pop-first
- Added test in main.go: alloc, free, alloc again — verifies same physical address returned
- Files changed: src/vm.go, src/main.go
- **Learnings for future iterations:**
  - allocPage() is called from 6 files (vm.go, main.go, scheduler.go, userspace.go, smp.go, gdt.go) — signature must stay unchanged
  - Free-list uses identity-mapped physical addresses as virtual pointers (phys=virt in this kernel)
  - All pre-existing demos (GC, VM, FS, timer, SMP, scheduler, userspace Ring 3) continue to pass after the change
---

## 2026-04-14 - US-002
- Implemented WaitQueue primitive: WaitQueue struct with fixed [16]uint32 FIFO array
- Added waitQueueSleep(wq): sets current task to taskBlocked, appends to queue, calls schedule()
- Added waitQueueWakeOne(wq): dequeues first task, sets to taskReady (interrupt-safe)
- Added waitQueueWakeAll(wq): sets all queued tasks to taskReady, resets count (interrupt-safe)
- Fixed schedule() to only set old task to taskReady if it was taskRunning (preserves taskBlocked)
- Files changed: src/scheduler.go
- **Learnings for future iterations:**
  - schedule() unconditionally set tasks[old].State = taskReady — must guard with state check to support blocking primitives
  - WaitQueue functions don't allocate or block, making wakeOne/wakeAll safe from interrupt context
  - Makefile only tracks main.go as Go dependency; must `make clean` before `make build` after changing other .go files
---

## 2026-04-14 - US-003
- Implemented yield() in src/scheduler.go: sets current task to taskReady and calls schedule()
- Modified demoTaskC to use yield() instead of hlt() in its spin-wait loop
- Files changed: src/scheduler.go
- **Learnings for future iterations:**
  - yield() follows the same pattern as waitQueueSleep (set state, call schedule) but sets taskReady instead of taskBlocked
  - Replacing hlt() with yield() causes much higher context switch frequency — task gives up CPU immediately instead of waiting for next timer interrupt
  - All 4 tasks (0-3) continue to round-robin correctly with yield-based voluntary switching mixed with timer-preempted switching
---

## 2026-04-14 - US-004
- Implemented taskSleep(ticks uint64): computes WakeupTick, inserts into sorted sleep queue, blocks task, calls schedule()
- Added sleep queue: fixed-size sorted array of task IDs (ascending by WakeupTick), with sleepQueueWakeExpired() called from timer handler
- Extended Task struct with WakeupTick uint64 field
- Updated handleTimer in pit.go to call sleepQueueWakeExpired(pitTicks) before EOI and schedule()
- Refactored demoTaskA (50 ticks), demoTaskB (75 ticks), demoTaskC (100 ticks) from spin-wait to taskSleep()
- Files changed: src/scheduler.go, src/pit.go
- **Learnings for future iterations:**
  - taskSleep inserts into sorted sleep queue before calling schedule() — timer handler only needs to check head entries
  - sleepQueueWakeExpired is interrupt-safe (no allocation, no blocking) — wakes all entries with WakeupTick <= now
  - Preemptive round-robin still works for task 0 (Ring 3 userspace) which doesn't call taskSleep
  - Demo tasks now truly block instead of spin-waiting — CPU is free for other tasks during sleep
---

## 2026-04-14 - US-005
- Created src/channel.go with Channel struct and chanCreate function
- Channel struct: fixed [256]uintptr ring buffer, readIdx/writeIdx/count/capacity, sender/receiver WaitQueues, used flag
- Static pool: [32]Channel array with chanPoolCount index; chanCreate allocates from pool sequentially
- chanCreate(capacity) initializes all fields, clamps capacity to 256, returns nil if pool exhausted; capacity=0 = unbuffered
- Files changed: src/channel.go (new)
- **Learnings for future iterations:**
  - Channel pool is pre-allocated statically — no heap allocation needed, safe for bare-metal
  - capacity=0 means unbuffered (rendezvous); send/recv logic in US-006 must handle this specially
  - WaitQueue type from scheduler.go is reused directly in Channel struct for sender/receiver blocking
  - Build succeeds and all existing demos boot correctly in QEMU with the new file included
---

## 2026-04-14 - US-006
- Implemented chanSend(ch, val) — blocking send with WaitQueue-based blocking when buffer full
- Implemented chanRecv(ch) — blocking receive with WaitQueue-based blocking when buffer empty
- Implemented chanTrySend(ch, val) — non-blocking send, returns false if full (interrupt-safe)
- Unbuffered (capacity=0) rendezvous: uses buf[0] + count as single-slot handoff between sender and receiver
- Buffered test: producer sends 10 values (0-9) on capacity-4 channel, consumer receives all in correct order — PASS
- Unbuffered test: receiver blocks, sender delivers 0xBEEF via rendezvous, receiver confirms — PASS
- Added 4 assembly address stubs in switch.S for chanProducerTask, chanConsumerTask, chanRendezvousA, chanRendezvousB
- Files changed: src/channel.go, src/switch.S, src/main.go
- **Learnings for future iterations:**
  - For unbuffered channels, spawn the receiver task before the sender so receiver blocks first — ensures clean rendezvous
  - chanSend/chanRecv use `for` loops around waitQueueSleep to handle spurious wakeups (re-check condition after waking)
  - chanTrySend and waitQueueWakeOne are interrupt-safe (no blocking, no allocation) — safe for IRQ handlers
  - With 8 tasks (0=userspace, 1-3=demo, 4-7=channel test), scheduler round-robin continues to work correctly
  - Task entry functions need //export annotation + matching assembly leaq stub in switch.S
---

## 2026-04-14 - US-007
- Implemented selectWait: multiplexed channel wait that blocks until any of multiple channels is ready
- Added SelectCase struct (ch, dir, val) and constants selectSend/selectRecv in src/channel.go
- Added chanRecvReady/chanSendReady (readiness checks) and chanRecvDirect/chanSendDirect (non-blocking execute)
- Added waitQueueAppend (add to WQ without blocking) and waitQueueRemove (remove specific task) to src/scheduler.go
- selectWait algorithm: check immediate readiness → if none, register on all WQs, block, deregister all on wakeup → loop
- Test: selector task selects on 2 buffered channels; producer A sends 0xAAAA to ch1 after 50 ticks, producer B sends 0xBBBB to ch2 after 100 ticks
- Serial confirms: case 0 dispatched for ch1, case 1 dispatched for ch2 — PASS
- Added 3 assembly address stubs in switch.S (selectTestTask, selectProducerA, selectProducerB)
- Files changed: src/channel.go, src/scheduler.go, src/switch.S, src/main.go
- **Learnings for future iterations:**
  - selectWait loops back to readiness check after wakeup to handle spurious wakeups (e.g., preemption between wakeOne and execution)
  - Remove from ALL wait queues after wakeup — even the one that woke us, since wakeOne already removed us, waitQueueRemove is a no-op if not found
  - chanRecvReady for both buffered and unbuffered is simply ch.count > 0 (for unbuffered, sender deposits value before blocking)
  - chanSendReady for unbuffered checks receiverWQ.count > 0 (needs a waiting receiver); for buffered, checks count < capacity
  - With 11 tasks (0=userspace, 1-3=demo, 4-7=channel, 8-10=select), maxTasks=16 still has headroom
  - VGA line 24 (last row) used for select test result
---

## 2026-04-14 - US-008
- Decomposed keyboard driver into IRQ handler + consumer task (first microkernel service)
- Defined KeyEvent struct (scancode uint8, ascii byte) packed into uintptr for channel transport
- Created keyboardChannel (capacity 16) via keyboardInit(), called before registering IRQ1 handler
- Simplified handleKeyboard: reads scancode, sends EOI, packs KeyEvent, calls chanTrySend (never blocks)
- Created keyboardConsumerTask: chanRecv loop handles backspace, enter, normal key VGA echo + serial logging
- Removed all VGA echo and serial logging logic from the IRQ handler — now lives only in consumer task
- Added assembly address stub in switch.S for keyboardConsumerTaskAddr
- Spawned keyboardConsumerTask as Task 11 in main.go
- Files changed: src/keyboard.go, src/switch.S, src/main.go
- **Learnings for future iterations:**
  - chanTrySend is the key primitive for interrupt→task communication: non-blocking, safe from IRQ context, drops if full
  - Packing small structs into uintptr avoids needing pointer-based channels: scancode|ascii<<8 fits in 2 bytes
  - keyboardInit() must be called before registerHandler(33, handleKeyboard) to avoid nil channel dereference in IRQ
  - With 12 tasks (0=userspace, 1-3=demo, 4-7=channel, 8-10=select, 11=keyboard), maxTasks=16 has 4 slots remaining
  - Consumer task follows same pattern as other tasks: sti(), log start, infinite loop with blocking recv
---

## 2026-04-14 - US-009
- Implemented serial output task as a microkernel service
- Created serialChannel (capacity 16) in src/serial.go
- Created serialTaskEntry: chanRecv loop receives uintptr (string pointer), writes bytes to COM1 via serialPutChar
- Implemented serialSend(msg string): stores msg in static pool slot, sends pointer via chanSend (blocking, task-context only)
- Direct serialPrint/serialPrintln preserved for interrupt context and early boot (before serialChannel exists)
- Added assembly address stub serialTaskEntryAddr in switch.S
- Spawned serialTaskEntry as Task 12 in main.go
- Files changed: src/serial.go, src/switch.S, src/main.go
- **Learnings for future iterations:**
  - Static string pool avoids heap allocation and GC issues: [16]string array with ring index, pointer to slot passed via channel
  - Pool size must match channel capacity (16) so chanSend blocks before ring wraps around consumed slots
  - serialSend is NOT safe from interrupt context (uses chanSend which can block) — use serialPrint/serialPrintln directly for IRQ handlers
  - With 13 tasks (0=userspace, 1-3=demo, 4-7=channel, 8-10=select, 11=keyboard, 12=serial), maxTasks=16 has 3 slots remaining
  - Same microkernel pattern as keyboard: dedicated task with sti() + infinite chanRecv loop
---

## 2026-04-14 - US-010
- Implemented filesystem task as a microkernel service with request-response IPC
- Defined FSRequest struct (op, name, data, replyCh) and FSResponse struct (ok, data, names)
- Created fsRequestChannel (capacity 8) and fsTaskEntry: chanRecv loop dispatches to fsCreate/fsWrite/fsRead/fsList, sends FSResponse on replyCh
- Implemented helper functions: fsSendCreate, fsSendWrite, fsSendRead, fsSendList — each creates reply channel, sends request, blocks on reply
- Created fsDemoTask that exercises all 4 helpers and verifies round-trip create/write/read/list
- Added assembly address stubs fsTaskEntryAddr and fsDemoTaskAddr in switch.S
- Spawned fsTaskEntry (Task 13) and fsDemoTask (Task 14) in main.go
- Kept direct FS demo in early boot for pre-scheduler verification; channel-based demo runs as a task
- Files changed: src/fs.go, src/switch.S, src/main.go
- **Learnings for future iterations:**
  - Request-response pattern: static pools for FSRequest/FSResponse (size matches channel capacity), unsafe.Pointer as uintptr through channels
  - Reply channels use capacity 1 so the FS task can send response without blocking even if requester hasn't called chanRecv yet
  - Each fsSend* helper consumes one channel from the pool (chanCreate) — with 32-slot pool, 8 helpers can be in-flight simultaneously
  - With 15 tasks (0=userspace, 1-3=demo, 4-7=channel, 8-10=select, 11=keyboard, 12=serial, 13=fs, 14=fs-demo), maxTasks=16 has 1 slot remaining
  - Direct fsCreate/fsWrite/fsRead/fsList still callable from task context but are internal to the FS task service
---

## 2026-04-14 - US-011
- Implemented syscall dispatch table with register-based ABI via int 0x80
- Modified isr.S: isr_common now passes pre-alignment RSP as 3rd arg (%rdx) = register frame pointer
- Updated go_interrupt_handler in interrupt.go: accepts 3 args (vector, errorCode, framePtr); vector 0x80 dispatches to syscallDispatch
- Defined SyscallFrame struct in userspace.go matching the 22-field isr_common stack layout (R15 through SS)
- Implemented syscallDispatch: reads frame.RAX as syscall number, dispatches to handler
- Implemented sys_yield (0): calls yield(), sets frame.RAX = 0
- Implemented sys_exit (1): sets current task to taskExited, calls schedule() — task never rescheduled
- Implemented sys_print (5): reads buffer from frame.RDI, length from frame.RSI, writes to serial byte-by-byte via serialPutChar
- Added taskExited = 3 constant to scheduler.go; schedule() naturally skips exited tasks (only matches taskReady)
- Invalid syscall numbers set frame.RAX = 0xFFFFFFFFFFFFFFFF (-1)
- Replaced 6-byte user program with 42-byte program: mov eax,5 / movabs rdi,string_addr / mov esi,18 / int 0x80 / jmp $ / "Hello from Ring 3!"
- Removed old syscallHandler function and registerHandler(0x80, syscallHandler) call; int 0x80 now bypasses the handler table
- Serial output confirms "Hello from Ring 3!" from user program's sys_print syscall
- Files changed: src/isr.S, src/interrupt.go, src/scheduler.go, src/userspace.go
- **Learnings for future iterations:**
  - Frame pointer = pre-alignment RSP in isr_common; writing to frame fields (e.g., frame.RAX) propagates back through popq + iretq
  - SyscallFrame field order must exactly match push order in isr_common (R15 first at offset 0, RAX at offset 112)
  - Syscall handler runs with IF=0 (interrupt gate), so only direct serialPrint/serialPutChar are safe — not serialSend (channel-based)
  - User program machine code: use movabs rdi,imm64 (48 BF + 8 bytes) for absolute 64-bit addresses in user space
  - String data embedded directly after code in the user code page; offset calculation: code_size bytes from userCodeVaddr
  - sys_exit + schedule() from int 0x80 context works correctly: abandoned task stack is never returned to
---

## 2026-04-14 - US-012
- Created src/elf.go with ELF64 parser: header and program header structs, validation, PT_LOAD extraction
- Defined Elf64Ehdr (Ident, Type, Machine, Entry, Phoff, Phentsize, Phnum) and Elf64Phdr (Type, Flags, Offset, Vaddr, Paddr, Filesz, Memsz, Align) structs
- Implemented elfParse(data []byte) → (entry, phdrs, ok): validates magic (0x7f ELF), ELFCLASS64, ELFDATA2LSB, ET_EXEC, EM_X86_64
- Returns entry point and slice of PT_LOAD program headers via static buffer (avoids memmove dependency)
- Added readU16LE/readU32LE/readU64LE little-endian byte-reading helpers
- Test in main.go: synthetic 120-byte ELF64 binary with one PT_LOAD segment, entry=0x401000 — PASS
- Serial confirms: parse OK, correct entry point, correct phdr vaddr/filesz/memsz
- Files changed: src/elf.go (new), src/main.go
- **Learnings for future iterations:**
  - TinyGo's sliceAppend for large structs requires memmove (not in stubs.S); use static fixed-size arrays instead of append
  - stubs.S provides memcpy and memset but NOT memmove — adding memmove would fix append for all struct types
  - ELF64 field offsets are fixed: e_entry at 24, e_phoff at 32, e_phentsize at 54, e_phnum at 56
  - Program header p_type at +0, p_flags at +4, p_offset at +8, p_vaddr at +16, p_paddr at +24, p_filesz at +32, p_memsz at +40, p_align at +48
  - elfParse returns a slice backed by a global buffer — caller must use data before calling elfParse again (for US-013 elfLoad)
  - All 15 tasks + userspace Ring 3 continue to work after adding ELF parser test to early boot sequence
---

## 2026-04-14 - US-013
- Implemented elfLoad(name string) bool in src/elf.go: reads ELF from FS via fsSendRead, maps PT_LOAD segments, allocates user stack, jumps to Ring 3
- Hand-crafted 162-byte ELF64 binary in buildUserElf(): ELF header + 1 PT_LOAD phdr + user code (sys_print + "Hello from Ring 3!")
- ELF binary stored in filesystem via direct fsCreate/fsWrite during early boot; loaded via fsSendRead at setupUserspace time
- elfLoad maps segment pages with pagePresent|pageWrite|pageUser, copies p_filesz bytes to vaddr, BSS zeroed by allocPage
- User stack: 2 pages (8 KiB) at 0x7FFF0000, mapped with user flags; stack top at 0x7FFF2000
- Replaced setupUserspace(): removed hardcoded 42-byte user program, now calls elfLoad("user.elf")
- Removed userCodeVaddr/userStackVaddr constants (addresses now come from ELF headers)
- Serial confirms: "ELF: loading user.elf, entry=0x40010078, 1 PT_LOAD segment(s)" → "Hello from Ring 3!"
- Files changed: src/elf.go, src/userspace.go, src/main.go
- **Learnings for future iterations:**
  - fsSendRead works from task 0 after schedReady=true: chanSend buffers the request, chanRecv blocks, scheduler picks up FS task (Task 13) which processes the request
  - Writing to newly mapped virtual addresses works without TLB invalidation — new mappings cause TLB miss, not stale entry; CPU walks page table on first access
  - ELF binary layout: headers mapped as part of PT_LOAD (p_offset=0), code starts at e_entry = p_vaddr + header_size (120 bytes); string address is relative to entry point
  - allocPage returns zeroed pages, so BSS (p_memsz - p_filesz) is implicitly handled — no explicit zeroing loop needed
  - Direct FS calls (fsCreate/fsWrite) work before scheduler for storing the ELF binary; channel-based fsSendRead works after scheduler for loading it
  - User stack at 0x7FFF0000 is well outside the 1 GiB identity map — no huge page conflicts
---
