diff --git a/src/internal/task/queue.go b/src/internal/task/queue.go
new file mode 100644
index 0000000..490a81c
--- /dev/null
+++ b/src/internal/task/queue.go
@@ -0,0 +1,146 @@
+package task
+
+import "runtime/interrupt"
+
+const asserts = false
+
+// gooos SMP spinlock — linked from the kernel's stubs.S.
+//
+//go:linkname gooos_spinlockAcquire spinlockAcquire
+func gooos_spinlockAcquire(lock *uint32)
+
+//go:linkname gooos_spinlockRelease spinlockRelease
+func gooos_spinlockRelease(lock *uint32)
+
+// Queue is a FIFO container of tasks. SMP v2: each queue has
+// its own spinlock for cross-CPU safety (work stealing, GC scan).
+// The zero value is an empty, unlocked queue.
+type Queue struct {
+	head, tail *Task
+	lock       uint32
+}
+
+// Push a task onto the queue. SMP-safe via spinlock.
+func (q *Queue) Push(t *Task) {
+	i := interrupt.Disable()
+	gooos_spinlockAcquire(&q.lock)
+	if asserts && t.Next != nil {
+		gooos_spinlockRelease(&q.lock)
+		interrupt.Restore(i)
+		panic("runtime: pushing a task to a queue with a non-nil Next pointer")
+	}
+	if q.tail != nil {
+		q.tail.Next = t
+	}
+	q.tail = t
+	t.Next = nil
+	if q.head == nil {
+		q.head = t
+	}
+	gooos_spinlockRelease(&q.lock)
+	interrupt.Restore(i)
+}
+
+// Pop a task off of the queue. SMP-safe via spinlock.
+func (q *Queue) Pop() *Task {
+	i := interrupt.Disable()
+	gooos_spinlockAcquire(&q.lock)
+	t := q.head
+	if t == nil {
+		gooos_spinlockRelease(&q.lock)
+		interrupt.Restore(i)
+		return nil
+	}
+	q.head = t.Next
+	if q.tail == t {
+		q.tail = nil
+	}
+	t.Next = nil
+	gooos_spinlockRelease(&q.lock)
+	interrupt.Restore(i)
+	return t
+}
+
+// Append pops the contents of another queue and pushes them onto the end of this queue.
+func (q *Queue) Append(other *Queue) {
+	i := interrupt.Disable()
+	gooos_spinlockAcquire(&q.lock)
+	gooos_spinlockAcquire(&other.lock)
+	if q.head == nil {
+		q.head = other.head
+	} else {
+		q.tail.Next = other.head
+	}
+	q.tail = other.tail
+	other.head, other.tail = nil, nil
+	gooos_spinlockRelease(&other.lock)
+	gooos_spinlockRelease(&q.lock)
+	interrupt.Restore(i)
+}
+
+// Empty checks if the queue is empty. SMP-safe via spinlock.
+func (q *Queue) Empty() bool {
+	i := interrupt.Disable()
+	gooos_spinlockAcquire(&q.lock)
+	empty := q.head == nil
+	gooos_spinlockRelease(&q.lock)
+	interrupt.Restore(i)
+	return empty
+}
+
+// Stack is a LIFO container of tasks.
+// The zero value is an empty stack.
+// This is slightly cheaper than a queue, so it can be preferable when strict ordering is not necessary.
+type Stack struct {
+	top *Task
+}
+
+// Push a task onto the stack.
+func (s *Stack) Push(t *Task) {
+	i := interrupt.Disable()
+	if asserts && t.Next != nil {
+		interrupt.Restore(i)
+		panic("runtime: pushing a task to a stack with a non-nil Next pointer")
+	}
+	s.top, t.Next = t, s.top
+	interrupt.Restore(i)
+}
+
+// Pop a task off of the stack.
+func (s *Stack) Pop() *Task {
+	i := interrupt.Disable()
+	t := s.top
+	if t != nil {
+		s.top = t.Next
+		t.Next = nil
+	}
+	interrupt.Restore(i)
+	return t
+}
+
+// tail follows the chain of tasks.
+// If t is nil, returns nil.
+// Otherwise, returns the task in the chain where the Next field is nil.
+func (t *Task) tail() *Task {
+	if t == nil {
+		return nil
+	}
+	for t.Next != nil {
+		t = t.Next
+	}
+	return t
+}
+
+// Queue moves the contents of the stack into a queue.
+// Elements can be popped from the queue in the same order that they would be popped from the stack.
+func (s *Stack) Queue() Queue {
+	i := interrupt.Disable()
+	head := s.top
+	s.top = nil
+	q := Queue{
+		head: head,
+		tail: head.tail(),
+	}
+	interrupt.Restore(i)
+	return q
+}
diff --git a/src/internal/task/task_stack.go b/src/internal/task/task_stack.go
index f566de0..1e6a379 100644
--- a/src/internal/task/task_stack.go
+++ b/src/internal/task/task_stack.go
@@ -30,6 +30,11 @@ type state struct {
 	// When initializing the goroutine, the stackCanary constant is stored there.
 	// If the stack overflowed, the word will likely no longer equal stackCanary.
 	canaryPtr *uintptr
+
+	// stackTop is the high address of the goroutine stack (canaryPtr +
+	// stackSize at initialize time). Used by gooos to set TSS.RSP0 on
+	// Ring-3 resume. See impldoc/phase_b_ring3_and_exec.md.
+	stackTop uintptr // gooos
 }
 
 // currentTask is the current running task, or nil if currently in the scheduler.
@@ -40,12 +45,19 @@ func Current() *Task {
 	return currentTask
 }
 
+//go:linkname gooosStackOverflow runtime.gooosStackOverflow
+func gooosStackOverflow(t uintptr)
+
 // Pause suspends the current task and returns to the scheduler.
 // This function may only be called when running on a goroutine stack, not when running on the system stack or in an interrupt.
 func Pause() {
 	// Check whether the canary (the lowest address of the stack) is still
-	// valid. If it is not, a stack overflow has occurred.
+	// valid. If it is not, a stack overflow has occurred. Call into gooos
+	// for a no-alloc diagnostic that survives the corrupted stack; the
+	// helper halts. Fall through to runtimePanic only as a defensive
+	// secondary if the gooos hook ever returns.
 	if *currentTask.state.canaryPtr != stackCanary {
+		gooosStackOverflow(uintptr(unsafe.Pointer(currentTask))) // gooos
 		runtimePanic("goroutine stack overflow")
 	}
 	if interrupt.In() {
@@ -80,6 +92,7 @@ func (s *state) initialize(fn uintptr, args unsafe.Pointer, stackSize uintptr) {
 	// the next stack switch, there was a stack overflow.
 	s.canaryPtr = (*uintptr)(stack)
 	*s.canaryPtr = stackCanary
+	s.stackTop = uintptr(stack) + stackSize // gooos
 
 	// Get a pointer to the top of the stack, where the initial register values
 	// are stored. They will be popped off the stack on the first stack switch
diff --git a/src/internal/task/task_stack_amd64.go b/src/internal/task/task_stack_amd64.go
index d252b1c..fe643e7 100644
--- a/src/internal/task/task_stack_amd64.go
+++ b/src/internal/task/task_stack_amd64.go
@@ -4,7 +4,18 @@ package task
 
 import "unsafe"
 
-var systemStack uintptr
+// gooosSMPMaxCPUs must match the kernel's maxCPUs (src/percpu.go).
+const gooosSMPMaxCPUs = 17
+
+// Per-CPU system stacks. Each CPU's scheduler uses its own stack
+// during task switching to avoid cross-CPU collisions.
+var systemStacks [gooosSMPMaxCPUs]uintptr
+
+// gooosCpuID returns the current CPU index from per-CPU storage.
+// Linked to the gooos kernel's cpuID() which reads %gs:0.
+//
+//go:linkname gooosCpuID cpuID
+func gooosCpuID() uint32
 
 // calleeSavedRegs is the list of registers that must be saved and restored when
 // switching between tasks. Also see task_stack_amd64.S that relies on the exact
@@ -44,18 +55,24 @@ func (s *state) archInit(r *calleeSavedRegs, fn uintptr, args unsafe.Pointer) {
 	r.r13 = uintptr(args)
 }
 
+//go:linkname gooosOnResume runtime.gooosOnResume
+func gooosOnResume()
+
 func (s *state) resume() {
-	swapTask(s.sp, &systemStack)
+	gooosOnResume() // gooos — update TSS.RSP0 for resumed goroutine
+	idx := gooosCpuID()
+	swapTask(s.sp, &systemStacks[idx])
 }
 
 func (s *state) pause() {
-	newStack := systemStack
-	systemStack = 0
+	idx := gooosCpuID()
+	newStack := systemStacks[idx]
+	systemStacks[idx] = 0
 	swapTask(newStack, &s.sp)
 }
 
-// SystemStack returns the system stack pointer when called from a task stack.
-// When called from the system stack, it returns 0.
+// SystemStack returns the current CPU's system stack pointer when
+// called from a task stack. Returns 0 when called from the system stack.
 func SystemStack() uintptr {
-	return systemStack
+	return systemStacks[gooosCpuID()]
 }
diff --git a/src/runtime/chan.go b/src/runtime/chan.go
index 269f5a0..ca78d9b 100644
--- a/src/runtime/chan.go
+++ b/src/runtime/chan.go
@@ -183,8 +183,8 @@ func (ch *channel) resumeRX(ok bool) unsafe.Pointer {
 		b.detach()
 	}
 
-	// push task onto runqueue
-	runqueue.Push(b.t)
+	// push task onto current CPU's runqueue
+	runqueues[gooosCpuID()].Push(b.t)
 
 	return dst
 }
@@ -210,8 +210,8 @@ func (ch *channel) resumeTX() unsafe.Pointer {
 		b.detach()
 	}
 
-	// push task onto runqueue
-	runqueue.Push(b.t)
+	// push task onto current CPU's runqueue
+	runqueues[gooosCpuID()].Push(b.t)
 
 	return src
 }
diff --git a/src/runtime/gc_blocks.go b/src/runtime/gc_blocks.go
index 7db6b7a..f730161 100644
--- a/src/runtime/gc_blocks.go
+++ b/src/runtime/gc_blocks.go
@@ -425,30 +425,27 @@ func runGC() (freeBytes uintptr) {
 	if baremetal && hasScheduler {
 		// Channel operations in interrupts may move task pointers around while we are marking.
 		// Therefore we need to scan the runqueue separately.
-		var markedTaskQueue task.Queue
-	runqueueScan:
-		for !runqueue.Empty() {
-			// Pop the next task off of the runqueue.
-			t := runqueue.Pop()
-
-			// Mark the task if it has not already been marked.
-			markRoot(uintptr(unsafe.Pointer(&runqueue)), uintptr(unsafe.Pointer(t)))
-
-			// Push the task onto our temporary queue.
-			markedTaskQueue.Push(t)
-		}
+		// SMP v2: scan all per-CPU runqueues. For now, only BSP runs GC;
+		// stop-the-world coordination across CPUs is deferred.
+		for rqIdx := 0; rqIdx < gooosSMPMaxCPUs; rqIdx++ {
+			var markedTaskQueue task.Queue
+		runqueueScan:
+			for !runqueues[rqIdx].Empty() {
+				t := runqueues[rqIdx].Pop()
+				markRoot(uintptr(unsafe.Pointer(&runqueues[rqIdx])), uintptr(unsafe.Pointer(t)))
+				markedTaskQueue.Push(t)
+			}
 
-		finishMark()
+			finishMark()
 
-		// Restore the runqueue.
-		i := interrupt.Disable()
-		if !runqueue.Empty() {
-			// Something new came in while finishing the mark.
+			i := interrupt.Disable()
+			if !runqueues[rqIdx].Empty() {
+				interrupt.Restore(i)
+				goto runqueueScan
+			}
+			runqueues[rqIdx] = markedTaskQueue
 			interrupt.Restore(i)
-			goto runqueueScan
 		}
-		runqueue = markedTaskQueue
-		interrupt.Restore(i)
 	} else {
 		finishMark()
 	}
diff --git a/src/runtime/interrupt/interrupt_gooos.go b/src/runtime/interrupt/interrupt_gooos.go
new file mode 100644
index 0000000..1a6297e
--- /dev/null
+++ b/src/runtime/interrupt/interrupt_gooos.go
@@ -0,0 +1,34 @@
+//go:build gooos && baremetal && kernelspace
+
+package interrupt
+
+//go:linkname gooos_readFlags readFlags
+func gooos_readFlags() uintptr
+
+//go:linkname gooos_restoreFlags restoreFlags
+func gooos_restoreFlags(flags uintptr)
+
+//go:linkname gooos_cli cli
+func gooos_cli()
+
+// Counter bumped by the gooos common ISR prologue / epilogue
+// (src/isr.S), defined as a .bss symbol there to avoid TinyGo's
+// dead-code elimination of Go-level vars only referenced from
+// assembly. Linkname target: unqualified "gooos_in_interrupt_depth".
+//
+//go:linkname gooos_inInterruptDepth gooos_in_interrupt_depth
+var gooos_inInterruptDepth uint32
+
+// State holds RFLAGS at the moment of Disable() so Restore() can
+// decide whether to re-enable interrupts.
+type State uintptr
+
+func Disable() State {
+	flags := gooos_readFlags()
+	gooos_cli()
+	return State(flags)
+}
+
+func Restore(state State) { gooos_restoreFlags(uintptr(state)) }
+
+func In() bool { return gooos_inInterruptDepth != 0 }
diff --git a/src/runtime/interrupt/interrupt_gooos_user.go b/src/runtime/interrupt/interrupt_gooos_user.go
new file mode 100644
index 0000000..8d64545
--- /dev/null
+++ b/src/runtime/interrupt/interrupt_gooos_user.go
@@ -0,0 +1,14 @@
+//go:build gooos && baremetal && !kernelspace
+
+// Userspace has no IRQs; Disable / Restore / In are trivial no-ops.
+// This file exists only so code under the (gooos && baremetal) tag
+// combo (runtime.addTimer, scheduler internals) resolves the
+// interrupt.* symbols at link time.
+
+package interrupt
+
+type State uintptr
+
+func Disable() State      { return 0 }
+func Restore(state State) {}
+func In() bool            { return false }
diff --git a/src/runtime/runtime_gooos.go b/src/runtime/runtime_gooos.go
new file mode 100644
index 0000000..722004e
--- /dev/null
+++ b/src/runtime/runtime_gooos.go
@@ -0,0 +1,91 @@
+//go:build gooos && baremetal && kernelspace
+
+// gooos-local runtime bodies for bare-metal x86_64 Ring 0.
+// Replaces the parts of runtime_unix.go incompatible with kernel
+// mode. The kernel defines putchar, pitTicks, and the cli/sti/hlt
+// primitives these stubs rely on.
+
+package runtime
+
+type timeUnit int64
+
+//go:linkname gooos_pitTicks main.pitTicks
+var gooos_pitTicks uint64
+
+// The kernel's assembly stubs in src/stubs.S use unqualified names
+// (no package prefix), so we link directly to those symbols.
+
+//go:linkname gooos_cli cli
+func gooos_cli()
+
+//go:linkname gooos_sti sti
+func gooos_sti()
+
+//go:linkname gooos_hlt hlt
+func gooos_hlt()
+
+// sleepTicks idles on the PIT counter until `d` ticks have elapsed.
+// The kernel's timer ISR increments gooos_pitTicks at 100 Hz.
+func sleepTicks(d timeUnit) {
+	deadline := gooos_pitTicks + uint64(d)
+	for gooos_pitTicks < deadline {
+		gooos_sti()
+		gooos_hlt()
+		gooos_cli()
+	}
+}
+
+func ticks() timeUnit { return timeUnit(gooos_pitTicks) }
+
+// 100 Hz PIT: one tick = 10 ms = 10_000_000 ns.
+func ticksToNanoseconds(t timeUnit) int64  { return int64(t) * 10_000_000 }
+func nanosecondsToTicks(ns int64) timeUnit { return timeUnit(ns / 10_000_000) }
+
+// tinygo_register_fatal_signals is provided by src/stubs.S (the
+// kernel has had this stub since before goroutine support). No Go
+// body needed here.
+
+// putchar writes a single byte to COM1. Used by runtime.printstring.
+//
+//go:linkname gooos_outb outb
+func gooos_outb(port uint16, value uint8)
+
+func putchar(c byte) { gooos_outb(0x3F8, c) }
+
+// buffered is a no-op on bare metal — no line-buffered stdio.
+func buffered() int { return 0 }
+
+// getchar is not used by gooos; return 0 to avoid pulling in stdin.
+func getchar() byte { return 0 }
+
+// exit / abort: kernel panic paths. No process to exit; halt forever.
+func exit(code int) {
+	for {
+		gooos_hlt()
+	}
+}
+
+func abort() {
+	for {
+		gooos_hlt()
+	}
+}
+
+// preinit is called by the runtime before initAll(). gooos's main.go
+// performs all hardware init in user main(), so preinit is a no-op.
+func preinit() {}
+
+// main is the C-ABI entry point called from boot.S after the
+// 32->64-bit bootstrap. run() initializes the heap, runs package
+// init functions, and invokes the user-written main in main.go.
+// If run() returns, halt forever.
+//
+//export main
+func main() {
+	preinit()
+	run()
+	exit(0)
+}
+
+// waitForEvents provided by wait_other.go (fallback). No override
+// needed — that fallback body is a no-op, acceptable here.
diff --git a/src/runtime/runtime_gooos_user.go b/src/runtime/runtime_gooos_user.go
new file mode 100644
index 0000000..0835d65
--- /dev/null
+++ b/src/runtime/runtime_gooos_user.go
@@ -0,0 +1,84 @@
+//go:build gooos && baremetal && !kernelspace
+
+// gooos userspace runtime bodies for Ring-3 user programs. The
+// kernel equivalent is runtime_gooos.go; the two files share the
+// (gooos && baremetal) tag prefix but are disambiguated by the
+// kernelspace tag on src/target.json. Primitives here route
+// through gooos syscalls exposed by user/rt0.S (syscall1 /
+// syscall3) rather than kernel asm stubs.
+
+package runtime
+
+import "unsafe"
+
+type timeUnit int64
+
+// Syscall numbers are fixed by the kernel dispatch table
+// (src/userspace.go): sys_exit=0, sys_write=1, sys_sleep=8.
+// The asm wrappers in user/rt0.S are reached via //go:linkname,
+// matching the pattern in user/gooos/syscall.go.
+
+//go:linkname userSyscall1 syscall1
+func userSyscall1(nr, a1 uintptr) uintptr
+
+//go:linkname userSyscall3 syscall3
+func userSyscall3(nr, a1, a2, a3 uintptr) uintptr
+
+// userTicks is a Go-side monotonic counter bumped whenever
+// sleepTicks returns. Coarse (10 ms resolution) but monotonic.
+// Good enough for time.Sleep; not a wall clock.
+var userTicks uint64
+
+// sleepTicks blocks this process on sys_sleep for d ticks, then
+// bumps the local tick counter. Called from the scheduler idle
+// path when every task is parked.
+func sleepTicks(d timeUnit) {
+	if d <= 0 {
+		return
+	}
+	userSyscall1(8, uintptr(d)) // sys_sleep
+	userTicks += uint64(d)
+}
+
+func ticks() timeUnit { return timeUnit(userTicks) }
+
+// 100 Hz PIT tick = 10 ms = 10_000_000 ns (matches kernel).
+func ticksToNanoseconds(t timeUnit) int64  { return int64(t) * 10_000_000 }
+func nanosecondsToTicks(ns int64) timeUnit { return timeUnit(ns / 10_000_000) }
+
+// putchar writes one byte via sys_write(buf, len, fd=1). fd=1 is
+// serial-only (fd=0 also routes to VGA but requires Ring-0 MMIO).
+// Used by runtime.printstring on panic paths.
+func putchar(c byte) {
+	b := c
+	userSyscall3(1, uintptr(unsafe.Pointer(&b)), 1, 1)
+}
+
+// buffered / getchar: no line-buffered stdio in userspace.
+func buffered() int { return 0 }
+func getchar() byte { return 0 }
+
+// exit routes to sys_exit. The kernel handler parks the
+// ring3Wrapper goroutine, so sys_exit never returns; the
+// trailing loop exists only to satisfy control-flow analysis.
+func exit(code int) {
+	userSyscall1(0, uintptr(code))
+	for {
+	}
+}
+
+func abort() { exit(1) }
+
+// preinit runs before initAll(). Nothing to do in userspace.
+func preinit() {}
+
+// main is the C-ABI entry point called from user/rt0.S:_start.
+// run() initializes the heap, runs package init funcs, and enters
+// the scheduler loop (seeded with main.main under scheduler=tasks).
+//
+//export main
+func main() {
+	preinit()
+	run()
+	exit(0)
+}
diff --git a/src/runtime/scheduler.go b/src/runtime/scheduler.go
index 30b2da8..a3eb9af 100644
--- a/src/runtime/scheduler.go
+++ b/src/runtime/scheduler.go
@@ -23,14 +23,26 @@ const asyncScheduler = GOOS == "js"
 
 var schedulerDone bool
 
-// Queues used by the scheduler.
+// gooosSMPMaxCPUs must match maxCPUs in the gooos kernel (src/percpu.go).
+const gooosSMPMaxCPUs = 17
+
+// Queues used by the scheduler. SMP v2: per-CPU runqueues replace
+// the single global runqueue. sleepQueue and timerQueue remain global
+// (protected by interrupt-disable on BSP; SMP-safe distribution deferred).
 var (
-	runqueue           task.Queue
+	runqueues          [gooosSMPMaxCPUs]task.Queue
 	sleepQueue         *task.Task
 	sleepQueueBaseTime timeUnit
 	timerQueue         *timerNode
 )
 
+// gooosCpuID returns the current CPU index. Linked to the gooos
+// kernel's cpuID() which reads %gs:0. On userspace builds this
+// returns 0 (single-CPU).
+//
+//go:linkname gooosCpuID cpuID
+func gooosCpuID() uint32
+
 // Simple logging, for debugging.
 func scheduleLog(msg string) {
 	if schedulerDebug {
@@ -75,9 +87,9 @@ func Goexit() {
 	deadlock()
 }
 
-// Add this task to the end of the run queue.
+// Add this task to the current CPU's run queue.
 func runqueuePushBack(t *task.Task) {
-	runqueue.Push(t)
+	runqueues[gooosCpuID()].Push(t)
 }
 
 // Add this task to the sleep queue, assuming its state is set to sleeping.
@@ -175,7 +187,7 @@ func scheduler() {
 			sleepQueueBaseTime += timeUnit(t.Data)
 			sleepQueue = t.Next
 			t.Next = nil
-			runqueue.Push(t)
+			runqueues[gooosCpuID()].Push(t)
 		}
 
 		// Check for expired timers to trigger.
@@ -190,7 +202,11 @@ func scheduler() {
 			tn.callback(tn, delay)
 		}
 
-		t := runqueue.Pop()
+		t := runqueues[gooosCpuID()].Pop()
+		if t == nil {
+			// SMP v2: try to steal work from other CPUs' runqueues.
+			t = stealWork()
+		}
 		if t == nil {
 			if sleepQueue == nil && timerQueue == nil {
 				if asyncScheduler {
@@ -245,7 +261,7 @@ func scheduler() {
 func minSched() {
 	scheduleLog("start nested scheduler")
 	for !schedulerDone {
-		t := runqueue.Pop()
+		t := runqueues[gooosCpuID()].Pop()
 		if t == nil {
 			break
 		}
@@ -257,6 +273,31 @@ func minSched() {
 }
 
 func Gosched() {
-	runqueue.Push(task.Current())
+	runqueues[gooosCpuID()].Push(task.Current())
 	task.Pause()
 }
+
+// stealWork attempts to steal a task from another CPU's runqueue.
+// Round-robin scan starting from the CPU after the current one.
+// Returns nil if no work is available on any other CPU.
+func stealWork() *task.Task {
+	me := gooosCpuID()
+	for i := uint32(1); i < gooosSMPMaxCPUs; i++ {
+		peer := (me + i) % gooosSMPMaxCPUs
+		t := runqueues[peer].Pop()
+		if t != nil {
+			return t
+		}
+	}
+	return nil
+}
+
+// apScheduler is the TinyGo scheduler entry for AP cores.
+// Called from gooos after per-CPU init (GS base, GDT, TSS,
+// LAPIC timer). Does not reinitialize the heap or call main.
+// The system stack for this AP is the stack we are running on
+// (the AP boot stack from smpInit). It will be saved into
+// systemStacks[cpuID()] on the first resume() call.
+func apScheduler() {
+	scheduler()
+}
